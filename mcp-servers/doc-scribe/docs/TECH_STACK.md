# Tech Stack

-   **Language**: Python 3.11+
-   **MCP Framework**: `mcp` SDK (stdio transport)
-   **Local LLM**: Ollama (`qwen2.5-coder:14b`)
-   **Networking**: `httpx` (async)
-   **Core Logic**: 
    -   `re` (Regex) for Markdown processing.
    -   `ast` for Python source analysis and manipulation.
